# future: pull Google Calendar / Gmail / GitHub via APIs
def fetch_calendar_for_user(user_id:str):
    return []

def fetch_emails_for_user(user_id:str):
    return []

def fetch_commits_for_user(user_id:str):
    return []
